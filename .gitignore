# ============================================================
# xju-api 单仓（含 new-api / CLIProxyAPI 源码）
# 各子项目自带 .gitignore 管其构建产物；此文件做全仓密钥保护。
# 注意：*.example / *.example.* 模板文件一律保留（开发需要）。
# ============================================================

# --- 真实密钥 / 运行期配置（绝不提交；模板 *.example.* 保留）---
**/config.yaml
**/config.local.yaml
**/config.k12.yaml
**/auths/
**/auths-k12/
**/logs-k12/
**/auth.json
.env
.env.local
**/.env
**/.env.local
*.pem
*.key
id_ed25519*
id_rsa*

# --- 构建 / 运行期产物 ---
node_modules/
dist/
new-api/prebuilt/
build/
.cache/
*.sqlite
*.sqlite-*
*.db
*.log
logs/

# --- OS / 编辑器 ---
.DS_Store
*.swp
.idea/
.vscode/

# 号池管理明文密钥(deploy/setup-pool-mgmt*.sh 生成,永不入库)
deploy/.pool-mgmt.env
deploy/.pool-mgmt-k12.env
**/.pool-mgmt-k12.env
